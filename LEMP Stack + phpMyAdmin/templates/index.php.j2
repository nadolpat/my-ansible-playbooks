<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Hello Ansible Nginx</title>
    <style>
        body { font-family: Arial; text-align: center; margin-top: 50px; background-color: #f0f0f0; }
        .container { background-color: #fff; padding: 30px; border-radius: 8px; box-shadow: 0 4px 8px rgba(0,0,0,0.1); display: inline-block; }
        h1 { color: #333; }
        p { color: #666; font-size: 1.2em; }
        .ip-address { font-weight: bold; color: #007bff; }
        .status { font-weight: bold; }
    </style>
</head>
<body>
    <div class="container">
        <h1>🚀 Hello This is NGINX!</h1>
        <p>You access from server:</p>
        <p class="ip-address">IP Address: <?= $_SERVER['SERVER_ADDR']; ?></p>
        <p>From Node: <span class="ip-address"><?= getenv('HOSTNAME') ?: '{{ inventory_hostname }}' ?></span></p>
        <p>Setup and Manage Ansible! ✨</p>
        <hr>
        <?php
        $db_host = 'localhost';
        $db_user = '{{ db_user }}';
        $db_pass = '{{ db_pass }}';
        $db_name = '{{ db_name }}';
        $conn = new mysqli($db_host, $db_user, $db_pass, $db_name);
        if ($conn->connect_error) {
            echo "<p class='status' style='color:red;'>Database connection failed: ".$conn->connect_error."</p>";
        } else {
            echo "<p class='status' style='color:green;'>Database connected successfully!</p>";
        }
        ?>
    </div>
</body>
</html>
